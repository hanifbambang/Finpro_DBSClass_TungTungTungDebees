# MongoDB Connection string (e.g. mongodb://localhost:27017/pokedex or Atlas URI)
MONGODB_URI=
# Redis Connection string (e.g. redis://default:password@host:port)
REDIS_URL=
# Secret for JWT signing
JWT_SECRET=
# Server port (keep different from the PostgreSQL project which runs on 3000)
PORT=3001
